defaults:
  - jump.yaml

batch_size: 256
num_workers: 8
pin_memory: False
prefetch_factor: 4

splitter:
  train: 90_000
  test: 10_000
  val: 5_000
  # retrieval: 3_000

split_path: ${paths.split_path}/bigger_jump_cl/
