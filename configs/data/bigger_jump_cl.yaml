defaults:
  - basic_jump.yaml

batch_size: 256
num_workers: 4
pin_memory: True
prefetch_factor: 2

splitter:
  _target_: src.data.splits.ScaffoldSplitter
  train: 25_000
  test: 3000
  val: 2000

split_path: ${paths.split_path}/bigger_jump_cl/
