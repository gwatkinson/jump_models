# @package _global_

defaults:
  - /experiment/final/img_size_experiments/default.yaml@_global_

data:
  transform:
    size: 512

model:
  embedding_dim: 512

  molecule_encoder:
    dropout: 0.3

  image_encoder:
    dropout: 0.3

trainer:
  devices: ???

logger:
  wandb:
    name: med_high_dropout
