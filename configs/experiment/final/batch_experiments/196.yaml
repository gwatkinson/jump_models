# @package _global_

defaults:
  - /experiment/final/batch_experiments/default.yaml@_global_

data:
  batch_size: 196   # Really similar to the ntxent loss run (should be the same)

trainer:
  devices: ???

logger:
  wandb:
    name: med_batch_size_196
