# @package _global_

defaults:
  - /experiment/final/optimizer_experiments/default.yaml@_global_
  - override /model/optimizer: lion.yaml

trainer:
  devices: ???

logger:
  wandb:
    name: med_lion
