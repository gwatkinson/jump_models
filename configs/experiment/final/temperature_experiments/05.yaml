# @package _global_

defaults:
  - /experiment/final/temperature_experiments/default.yaml@_global_

model:
  criterion:
    temperature: 0.5  # Could be skipped as it is the default value

trainer:
  devices: ???

logger:
  wandb:
    name: med_temperature_05
