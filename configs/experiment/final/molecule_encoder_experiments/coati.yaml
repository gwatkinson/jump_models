# @package _global_

defaults:
  - /experiment/final/molecule_encoder_experiments/default.yaml@_global_
  - override /model/molecule_encoder: coati.yaml

trainer:
  devices: ???

logger:
  wandb:
    name: med_coati
