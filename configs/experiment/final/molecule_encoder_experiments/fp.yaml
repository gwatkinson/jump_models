# @package _global_

defaults:
  - /experiment/final/molecule_encoder_experiments/default.yaml@_global_
  - override /model/molecule_encoder: fp_encoder.yaml

trainer:
  devices: ???

logger:
  wandb:
    name: med_fp_encoder
